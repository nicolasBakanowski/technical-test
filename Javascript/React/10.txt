How do I import the following component, considering that it is inside a
file in the same directory, called MyComponent.js?
- other en el ejemplo que pasaroon esta usando una etiqueta h1, por lo tanto la extencion del archivo debe ser jsx